# Changelog

All notable changes to this project are documented in this file, generated from
[Conventional Commits](https://www.conventionalcommits.org) history with [git-cliff](https://git-cliff.org).
This project does not use tagged releases; commits are grouped chronologically under "Unreleased".

Automatically regenerated by CI on every push to `main` (see `.github/workflows/changelog.yml`).
To regenerate locally, run `npx --yes git-cliff --config cliff.toml --output CHANGELOG.md`.
## [unreleased]

### 🚀 Features

- *(map)* Highlight visible cluster when hovering a clustered photo
- *(sync)* Elevation hover highlights nearest photo in storyline and map

### 🐛 Bug Fixes

- *(map)* Pass captured_at timestamp on marker hover for elevation profile sync
- *(coverage)* Suppress unreachable defensive branches in ElevationPanel

### 📚 Documentation

- Update roadmap — v2 delivered, v3 planned with timeline design
- Move folder watch and RAW support to v3
- Update test counts (208 backend, 140 frontend) and coverage (96% frontend)
- Update frontend test count (146) and coverage (97%) in README
- Add screenshots section and emoji headings to README
- Add screenshots, emoji headings and updated counts to README.fr.md; commit screenshot assets
- Add code of conduct
- Add contribution guide
- Add security policy
- Remove unused frontend Vite scaffold README

### 🎨 Styling

- *(map)* Stronger cluster highlight — orange background + glow shadow
- *(elevation)* Unify cursor color and show altitude on sync cursor
- *(elevation)* Use light grey for both cursors and altitude text

### ⚙️ Miscellaneous Tasks

- Remove paths-ignore on pull_request — ensures CI runs on docs PRs so Codecov can post patch status
## [2.0.0] - 2026-05-21

### 🚀 Features

- *(ui)* Storyline ↔ map highlight synchronisation (V2-01)
- Add unquested photos panel (V2-02)
- *(ui)* Add all-photos browser panel with orphan filter (V2-03)
- *(map)* Add OSM layer alternatives with per-layer tile cache (V2-04)
- *(map)* Elevation profile panel with storyline sync (V2-05)

### 🐛 Bug Fixes

- *(tests)* Add missing unquested field to StatusDashboard test fixtures
- *(map)* Use /tiles/ prefix (no /api) for tile URL — tiles router has no prefix
- *(elevation)* Shift trackpoint timestamps by camera UTC offset for correct photo sync

### 📚 Documentation

- Update roadmap — reorder v2, add v3 section, mark completed items

### 🎨 Styling

- *(ui)* Increase contrast on storyline highlighted item
- *(ui)* Add hover effect and full-item click on all-photos panel
- *(ui)* Align all-photos hover color with QuestList (#2a2a4e)
- *(ui)* Align storyline highlighted item color with hover palette (#2a2a4e)
- *(elevation)* Use light grey for storyline hover cursor on dark background

### 🧪 Testing

- *(frontend)* Improve coverage on new v2 components
- *(frontend)* Cover missing branches in quests store
- *(elevation)* Cover highlightedX computed and onMousemove handler
## [1.0.0] - 2026-05-20

### 🚀 Features

- *(core)* Implement database, config and filesystem helpers (Phase 1A)
- *(api)* Implement FastAPI server skeleton, status endpoint and SSE bus (Phase 1B)
- *(frontend)* Implement Vue skeleton with Leaflet map, StatusDashboard and OSM tile proxy (Phase 1C)
- *(import)* Implement EXIF extraction worker and import service (Phase 2A)
- *(import)* Implement GPX parser worker and import service (Phase 2B)
- *(quests)* Implement quest clustering service and GET /api/quests endpoint (Phase 2C)
- *(import)* Implement GPS interpolation from GPX trackpoints (Phase 2D)
- *(import)* Add camera UTC offset support for GPS interpolation
- *(import)* Implement elevation enrichment via OpenTopoData (Phase 2E)
- *(api)* Implement import pipeline API and reindex endpoint (Phase 2F)
- *(thumbs)* Implement thumbnail generation worker, service and API endpoints (Phase 3)
- *(map)* Implement photo list API, geocode search, map markers and quest sidebar (Phase 4)
- *(map)* Recentre map on quest bbox when quest is selected
- *(import)* Generate thumbnails automatically at end of import pipeline
- *(map)* Lightbox, localized date+time and photo index in popups (P4-FU-01/02/03)
- *(map)* Display GPX trace as polyline when quest is selected (P4-FU-04)
- *(map)* Display GPX trace as separate polylines per GPX file (P4-FU-04)
- *(api)* Add GET /api/quests/{id}/photos endpoint (P5-01)
- *(frontend)* Add QuestStoryline panel with scrollable photo list (P5-02)
- *(api,frontend)* Add quest renaming via PATCH /api/quests/{id} (P5-03)
- *(api)* Add GET /api/quests/{id}/gpx endpoint (P5-04)
- *(api)* Add PATCH /api/photos/{id} for manual GPS placement (P6-02)
- *(api)* Add POST /api/quests/{id}/place for mass orphan placement (P6-07)
- Photo deletion, deleted_photos blocklist and map placement mode (P6-08/09/10)
- *(ui)* CSS grid layout with minmax columns for quest and storyline panels

### 🐛 Bug Fixes

- *(core)* Close SQLite connections properly to avoid ResourceWarning
- *(events)* Send connected event on subscribe and heartbeat every 30s
- *(events)* Send connected event on subscribe and heartbeat every 30s
- *(cli)* Build frontend automatically on stamped start if dist/ is missing
- *(quests)* Apply camera UTC offset when matching GPX files to quests
- *(test)* Close unawaited coroutine in fast_timeout mock to suppress RuntimeWarning
- *(frontend)* Fix TypeScript errors in test files caught by vue-tsc
- *(quests)* Compute quest bbox from GPX trackpoints and EXIF GPS in one pass
- *(import)* Prevent GPS interpolation across different GPX files
- *(lightbox)* Serve original photo file instead of thumbnail
- *(frontend)* Fix TypeScript errors in quests store tests
- *(api)* Recompute quest bbox after orphan placement (P6-07)
- *(map)* Apply crosshair cursor via JS on placement mode activation
- *(ui)* Visual distinction for orphan photos in storyline panel
- *(ui)* Restyle pin button — red circular hover border, opacity-based orphan distinction
- *(ui)* Replace invisible trash emoji with styled ✕ delete button
- *(ui)* Enforce square dimensions on action buttons for true circular border
- *(status,ui)* Live status counts and real-time refresh after photo delete
- *(ui)* Remove Vue template boilerplate CSS blocking full-width layout

### 🚜 Refactor

- *(imports)* Remove stale system_state writes now that status reads live from DB

### 📚 Documentation

- Add public architecture reference
- Add technology and coverage badges, update phase 2 status
- Rewrite README.md and add README.fr.md

### 🎨 Styling

- *(ui)* Enforce 12px minimum font size across all components

### 🧪 Testing

- *(frontend)* Achieve 100% coverage on api, MapView and status store
- *(backend)* Achieve 100% backend coverage and rename coverage badge labels
- *(frontend)* Increase MapView.vue coverage to 100% statements
- *(backend)* Cover uncovered patch lines to meet 90% patch target
- *(frontend)* Add PhotoLightbox component tests
- Cover placement click handler, cursor watch and geolocated photo median

### ⚙️ Miscellaneous Tasks

- *(setup)* Initialize project structure and tooling for Phase 0
- *(test)* Show individual test results and backend coverage in terminal
- *(frontend)* Remove scaffold icon components
- *(frontend)* Remove vite-plugin-vue-devtools to fix npm ci on Node 20
- Upgrade Node from 20 to 22 to fix npm ci lock file mismatch
- Upgrade npm to v11 before npm ci to match local lock file format
- Replace npm ci with npm install to fix lock file version mismatch
- Restore npm ci via corepack and packageManager field
- *(frontend)* Remove oxlint and restore npm ci via corepack
- Use npm install instead of npm ci for frontend jobs
- Ignore data-samples/ directory
- *(ci)* Fix Codecov flag uploads and coverage path resolution
- *(ci)* Add verbose and fail_ci_if_error to Codecov uploads for debugging
- Trigger CI after Codecov app authorization
- *(ci)* Tune Codecov targets and remove debug flags
- *(ci)* Disable carryforward on Codecov flags to unblock project status check
- *(ci)* Switch Codecov upload from CODECOV_TOKEN to OIDC
- *(ci)* Add after_n_builds and set project check as informational
- *(ci)* Revert to CODECOV_TOKEN — OIDC breaks codecov/patch
- *(test)* Restrict frontend coverage report to .ts and .vue files
- Trigger CI rerun
- Trigger CI rerun
- *(ci)* Set codecov/patch to success when no coverage report is found
- *(ci)* Set codecov/patch to informational
